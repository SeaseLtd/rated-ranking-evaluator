[project]
name = "commons"
version = "0.1.0"
description = "Common library for shared functions and classes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"
